{
    "name": "interactive-us-county-map",
    "version": "1.0.0",
    "description": "Interactive US County Map application",
    "type": "module",
    "scripts": {
        "dev": "npx --no-install vite --config vite.config.mjs",
        "build": "npx --no-install vite build --config vite.config.mjs",
        "preview": "npx --no-install vite preview --config vite.config.mjs"
    },
    "devDependencies": {
        "vite": "^5.0.10"
    }
}
